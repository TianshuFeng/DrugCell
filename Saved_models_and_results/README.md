# Saved Models and Results

The model files in this folder are stored in the `Save` section of the **Release** page under the archive named `Save.rar`.

To restore the saved models and results:
1. Download `Saved_models_and_results.part01.rar`... from the Release page.  
2. Extract all files into the current `Saved_models_and_results` folder.  


